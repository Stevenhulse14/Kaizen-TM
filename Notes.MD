Thing that I have noticed first the project is all in the frontend
that means no sperate backend and also all data is stored in the browser.
this include imgs which can slowdown the app.
We found a bug in the search filter where the max price is not working as expected.
the first piece of the bug is that the max price is not working as expected.
the second part is in the api where the max price is not being used correctly.
